<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.learnkotlin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LearnKotlin">
        <activity android:name=".MarsActivity"/>
        <activity android:name=".UnscrambleActivity"/>
        <activity android:name=".DessertClickerScreen"/>
        <activity android:name=".WordGameScreen" />
        <activity android:name=".AffirmationsScreen" />
        <activity android:name=".TipCalculate" />
        <activity android:name=".DiceScreen" />
        <activity android:name=".BirthdayScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>